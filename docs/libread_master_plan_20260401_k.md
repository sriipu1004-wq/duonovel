# LIB read 親チャット正史メモ 2026-04-01 k

更新日: 2026-04-01
用途: 親チャットで固めた方針・優先順位・運用ルールの統合正史ファイル
配置: `docs/libread_master_plan_20260401_k.md`
扱い:
- 以後の正史参照は、**まずこのファイルを優先**する
- `docs/libread_master_plan_20260401_j.md` は直前版の参考履歴として残してよい
- 正史更新が必要な時は、正史ファイルと更新内容を吟味して新しい正史ファイルを作り直す方式を使ってよい
- 古い正史ファイルは量が増えた時点でまとめて削除してよい

---

## 1. 正式名称
- サービス正式名称は **LIB read（ライブリード）**
- 旧暫定名「デュオノベル」は過去差分や旧文面に残っている場合があるが、新規作業では **LIB read** を正式名称として扱う

---

## 2. 公開モデルの固定仕様
- 作品規模では **公開 / 非公開** を扱う
- エピソード規模では **投稿 / 予約投稿 / 下書き保存** を扱う
- **エピソード規模での公開 / 非公開という考え方は廃止** する
- series 側 canonical は `publication_status`
- episodes 側 canonical は `posting_status`, `scheduled_for`, `posted_at`, `last_edited_at`
- 既存の `is_published / published` は移行期互換として扱う
- 前話が下書きの時は次話を 投稿 / 予約投稿 にできない制約を持たせる
- 前話が予約投稿の時は次話の予約時刻を前話より前にできない制約を持たせる
- 投稿済みエピソードの編集は許可する
- 作品ページでは各話に投稿日を出し、編集済みなら投稿日の隣に編集日と編集済み表示を出す

---

## 3. 再生体験と閲覧演出の固定仕様

### 3-1. 再生追従
- 線が文字を追従する機能を維持・修正する
- 閲覧位置が自動でスクロールする機能を維持・修正する
- 将来的には文単位 timestamp に寄せる方向を前提にする

### 3-2. 次話自動再生
- 次話自動再生ボタンを配置し、ON/OFF を切り替えられるようにする
- **朗読が最後まで終了した時だけ** 次話自動再生が効く
- 手動の前話 / 次話遷移では autoplay しない

### 3-3. 演出反映
- 演出設定は少なくともプレビューどおりに作品閲覧ページへ反映されることを目標にする
- 静的な見た目反映は完了済み
- scene cue の最小 runtime 発火も完了済み
- 残りは scene_break 差し込み、文単位 timestamp 同期、cue ごとの詳細な BGM フェード制御、動的演出種類の追加とする

### 3-4. 作品閲覧ページフッターの固定仕様
- 作品閲覧ページの朗読フッターは **中央揃え** で、**再生ボタンが真ん中** に来る構成へ統一する
- 左から次の順で配置する
  - **栞**
  - **倍速**
  - **前話**
  - **再生**
  - **次話**
  - **自動追尾**
  - **設定**
- フッター内のそれ以外の要素は削除する
- **「現在位置に戻る」は採用しない**。この位置は **倍速** に置き換える
- **自動追尾中は反応無し** とする
- 朗読停止中は朗読に直接関係する footer 要素を隠してよい

### 3-5. 設定画面の固定仕様
- 設定画面表示中は本文を非表示にしてよい
- 設定画面を閉じた時は元のスクロール位置へ戻る
- 表示演出欄は現段階では
  - 全演出を非表示
  - 文字サイズ
  - 行間
  のみに限定する
- 朗読欄には少なくとも
  - 朗読音量
  - 次話自動再生
  - 朗読停止
  を置く

---

## 4. 作者向け / 演出の固定方針
- 作者向け主導線は **作品ワークスペース中心**
- `/write` = 作品ワークスペース一覧
- `/write/series/[seriesId]` = 作品ワークスペース本体
- `/manage` と `/manage/series/[seriesId]` は backward 互換 redirect
- `WriteEpisodeForm.tsx` は **本文編集中心**
- 演出は **既定演出設定** と **演出編集** に責務分離して扱う
- **既定演出設定** は作品共通の既定値を置く場所
  - 共通BGM
  - 既定フォント
  - 既定文字色
  - 既定背景
  - 作品共通のプレビュー確認
- **各話演出編集** はその話限定の設定を置く場所
  - 各話BGM
  - その話限定の背景
  - 文字演出
  - 挿絵
  - 場面転換
  - その他その話固有の演出
- 今後の機能追加でも、この割当を崩さない

---

## 5. 子チャット運用の正式テンプレ
- 子チャットの役割は **VS Code local ファイル変更指示を出すこと**
- 実際に反映・実行するのはユーザー側
- GitHub 上に存在しても、VS Code local に無いファイルを前提に実装を進めない
- repo にあること、local にあること、push 済みであることを分けて確認する
- repo と VS Code local にズレがあったら、その場で再判定を出す
- 正式な報告先は親チャットのみ
- 子チャットから別の子チャットへの正式引き継ぎ文は作らない
- チャットが重くなった場合も、子チャットは親チャット向け報告のみを出す
- 次の子チャットへ渡す正式な引き継ぎ文は親チャットが作る
- 必ず repo 実体を確認して、前後の形に厳密に合わせる
- 差し込み位置が曖昧なら、部分差し込みではなく丸ごと置換用の完成コードを優先する
- 同期判定は途中表示ではなく **最終実測値** を優先する

---

## 6. 実装済みの主要土台
- 作品ワークスペース導線統合
- 作品ワークスペース本体
- 本文編集ページの本文中心UI整理
- 作品ワークスペース一覧 / BGM管理導線整理MVP
- 演出付き本文プレビューMVP
- BGM責務整理MVP
- 作品公開投稿フローMVP
- 閲覧演出反映整合MVP
- 朗読再生追従UI修正MVP
- 動的演出発火整備MVP
  - scene cue の runtime 発火処理を追加
  - triggerText を含む文を基準に cue を sentence index へ解決する処理を追加
  - 発火済み cue を管理し、同一進行方向では重複発火しないように変更
  - 巻き戻し時は cue 状態をリセットして再評価するように変更
  - cue に backgroundPreset がある場合、runtime で背景テーマを差し替える入口を追加
  - cue に nextBgmAudioPath / nextBgmTitle がある場合、runtime で BGM を差し替える入口を追加
  - 現在発火中の cue ラベルを UI 上で見えるように変更
- 朗読許可3状態
- 承認フロー最小版
- `/record` 朗読ハブ最小版
- `/record/create/[seriesId]` 制作入口最小版
- 音声アップロード二段階チェック土台
- 朗読検索UI強化MVP
- 作品ブックマーク最小版
- 反応機能名称整理MVP
- レビュー最小版
- レビュー反応拡張MVP
- エピソードコメント導線MVP
- 反応表示制御MVP

---

## 7. 現在の優先順位（2026-04-01 scene cue runtime 発火反映版）
### 最優先
**scene_break と timestamp 精度改善**
- scene_break の本文中差し込み
- 文単位 timestamp 保存の型と保存経路の整備
- 文単位 timestamp に基づく線追従精度改善
- 動的発火の sentence index 推定依存を縮小する

### 2位
**動的演出の詳細化**
- cue ごとの詳細な BGM フェード制御
- 動的演出種類の追加
- autoplay と dynamic cue の相互作用確認
- 停止中 UI と動的演出の整合確認

### 3位
**episodes / recordings / policy の repo 正史整備**
- migration の棚卸し
- policy の棚卸し
- repo / DB 差分の整理
- 今後の追加作業で事故らないようにする

### 4位
**他小説投稿サイト参考機能の追加整理**
- なろう / ハーメルン等を参考にした導線整理
- 追加した方がいい最小機能の整理
- 何を参考にし、何を差別化するかの線引き

### 5位
**ロゴ / アイコン導入**
- ヘッダー反映
- ブランド統一
- 画像仕様が揃い次第着手

---

## 8. 同期と docs 運用
- GitHub にあるだけで local に無い状態は、正史確認には使えても実作業継続前提にはしない
- 次の作業前に必ず local を同期する
- 用語を分ける
  - repo正史反映済み
  - local反映済み
  - 両方反映済み
- 「反映済み」は原則、GitHub と local の両方が揃った時だけ使う
- docs cleanup は毎回必須ではない
- 重複 docs が新たに増えた時だけ cleanup 対象にする

---

## 9. このファイルの扱い
- 親チャットで方針や優先順位が更新されたら、このファイル系統も更新する
- ユーザーが改善点・変更点を送ったら、原則このファイルへ統合する
- 子チャットへ前提を渡す時は、このファイルの内容を正史として扱う
- チャット本文だけで持たず、repo 内ファイルも基準にする
- 以後の正史参照は、原則このファイルを最新基準とする